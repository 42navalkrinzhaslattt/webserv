server {
    listen 127.0.0.1:8080;
    server_name localhost;
    root .;

    # Default index files
    index empty.html;

    # Maximum client body size (10MB)
    client_max_body_size 10m;

    # Root location
    location / {
        # Allow GET method
        limit_except GET;
    }
}
