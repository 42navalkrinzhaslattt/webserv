# Test specific error cases
server {
    # Empty server_name value
    server_name;

    # Invalid port number
    listen 999999;

    # Missing semicolon
    root /tmp/www

    # Invalid location block format
    location /test
        index index.html;
    }
}
